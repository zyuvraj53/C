#include<stdio.h> 
#include<stdlib.h> 

int SumOfElements(int A[]){
  int i, sum = 0;
  for(i = 0; i < sizeof(A); i)
}

int main(){
  int A[] = {1, 2, 3, 4, 5};
  int total = SumOfElements(A);
  int size = sizeof(A);
} 