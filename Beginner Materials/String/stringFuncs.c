#include <stdio.h>
#include <string.h>

int main(){
  // char strarray[80];
  // scanf("%79s", strarray);
  // printf("%i",strlen(strarray));

  char stra[] = "hello ";
  char strb[] = "there";
  char strc[100] = strcat(stra, strb);
  //strc = ;

  printf("%s", strc);

  // char* a = "hello there hi hello";
  // printf("%i", strlen(a));


}